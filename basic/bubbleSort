
int [] array=new int [89,7,2,45,1,4];
public void bubbleSort(){
    for (int i=0; i < 6; i++) {
        if (array[i] < array[i+1]) {
        } else if (array[i] > array[i+1]) {
            int num = array[i];
            array[i] = array[i+1];
            array[i+1] = num;
        }
    }
}  
